課題名           : max
提出ファイル      : max.c
使用可能関数      :
--------------------------------------------------------------------------------

次の関数を作成してください:

int		max(int* tab, unsigned int len);

1つ目の引数は `int` の配列、2つ目の引数はその配列の要素数です。

この関数は、配列内で見つかった最大の数値を返します。

配列が空の場合、関数は `0` を返します。
